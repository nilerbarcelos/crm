<h1> <%= (t 'documents.list').concat @parent.name %></h1>

<table>
  <tr>
    <th><%= t 'patterns.title' %></th>
    <th>Download</th>
  </tr>

<% for document in @documents %>
  <tr>
    <td><%=h document.title %></td>
    <td><%= link_to "Download", document.doc.url %></td>
    <td><%= link_to (t 'patterns.show'), [@parent, document] %></td>
    <td><%= link_to (t 'patterns.edit'), edit_polymorphic_path([@parent, document]) %></td>
    <td><%= link_to (t 'patterns.destroy'), [@parent, document], :confirm => (t 'patterns.confirm'), :method => :delete %></td>
  </tr>
<% end %>
</table>


<br />

<%= link_to (t 'documents.new'), new_polymorphic_path([@parent, :document]) %>

