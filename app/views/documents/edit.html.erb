<h1> <%= (t 'documents.list').concat @parent.name %></h1>

<h2><%= (t 'documents.edit') %></h2>

<%= render :partial => "form", :locals => {:type => (t 'patterns.update')} %>

| <%= link_to (t 'patterns.back'),polymorphic_url([@parent, :documents]) %>
