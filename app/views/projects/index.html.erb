<h1><%= t 'projects.list' %></h1>

<table>
  <tr>
    <th><%= t 'patterns.image' %></th>
    <th><%= t 'patterns.name' %></th>
    <th><%= t 'patterns.description' %></th>
    <th><%= t 'projects.customer' %></th>
    <th><%= t 'projects.active' %></th>
    <th><%= t 'patterns.documents' %></th>
  </tr>

<% for project in @projects %>
  <tr>
    <td><%= image_tag project.photo.url(:very_small) %></td>
    <td><%=h project.name %></td>
    <td><%=h project.description %></td>
    <td><%=h project.customer.name %></td>
    <td><%=h project.active %></td>
    <td><%= link_to (t 'patterns.documents'), project_documents_path(project) %></td>
    <td><%= link_to (t 'patterns.show'), project %></td>
    <td><%= link_to (t 'patterns.edit'), edit_project_path(project) %></td>
    <td><%= link_to (t 'patterns.destroy'), project, :confirm => (t 'patterns.confirm'), :method => :delete %></td>
  </tr>
<% end %>
</table>

<%= link_to (t 'projects.new'), new_project_path %>
